# Linked List Visualizer
This is a linked list visualizer that displays the way data is stored behind a linked list. The user can manipulate with the list using the UI.

# Functions
* Adding an element
	* Add to front/back
	* Add at index
* Removing an element
	* Remove by value
	* Remove first/last
* Searching
	* Search by value
	* Search by index
* Iterating through the linked list
* Reversing all the contents of the list with a single button

# Reflection
Through the process, I learned that AI is extremely useful when writing code, in fact, because AI was writing the code, I didn't need to write any code myself. A clear benefit of working with AI is the efficiency. This linked list visualizer took me less than 30 minutes to make with AI. I would expect myself to spend at least an hour working on this if I didn't use AI, showing the efficiency of working with AI. Before working on this, I thought AI would cause a lot of problems because it would make mistakes in the code and I couldn't even understand it, but working with Claude was a better experience than I anticipated it to be. Obviously as a student, I realize a significant drawback for working with AI is I don't learn anything. Overall, AI is extremely useful, it can really boost efficiency.

### Note: Code contains AI generated content
